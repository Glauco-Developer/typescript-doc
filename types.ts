let a: number;
let b: string;
let c: boolean;
let d: any;
let e: number[] = [1, 2, 3];

// enum allow us to defined a set of named constants.
// Typescript provides both numeric and string-based enums.
enum Color { Red = 0, Green = 1, Blue = 2 };
let backgroundColor = Color.Blue;